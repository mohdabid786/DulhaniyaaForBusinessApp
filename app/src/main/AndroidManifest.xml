<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dulhaniyaa.dulhaniyaaforbusinessapp">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.Splash"
            android:theme="@style/TranparentToolbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/TranparentToolbar" />
        <activity android:name=".activities.Login" />
        <activity android:name=".activities.SignUp" />
        <activity android:name=".activities.BasicInformation" />
        <activity
            android:name=".activities.Home"
            android:label="@string/title_activity_drawer"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.OTP" />
        <activity android:name=".activities.BusinessInformation" />
        <activity android:name=".activities.Setting" />
        <activity android:name=".activities.MembershipPackages" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="fcce1eb7518ccc8b21eda4ccbcea63e429e31f39" />

        <activity android:name=".activities.DulhaiyaaUpdates"></activity>
    </application>

</manifest>